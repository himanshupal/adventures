{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/target/debug/dsa",
      "cargo": {
        "args": ["build", "--bin=dsa", "--package=dsa"],
        "filter": {
          "name": "dsa",
          "kind": "bin"
        }
      },
      "cwd": "${workspaceFolder}"
    }
  ]
}
